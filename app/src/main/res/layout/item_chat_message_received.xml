<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <!-- 날짜 헤더 -->
    <TextView
        android:id="@+id/text_date_header_received"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="6dp"
        android:padding="6dp"
        android:text="2024-05-01"
        android:textSize="12sp"
        android:textColor="@color/trust_text_secondary"
        android:background="#eeeeee"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="visible" />

    <!-- 🔥 상대방 프로필 이미지 -->
    <ImageView
        android:id="@+id/image_profile_received"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="6dp"
        android:background="@drawable/chat_avatar_background"
        android:scaleType="centerCrop"
        android:src="@drawable/no_profile"
        app:layout_constraintTop_toBottomOf="@id/text_date_header_received"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- 상대방 닉네임 -->
    <TextView
        android:id="@+id/text_nickname_received"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@id/image_profile_received"
        app:layout_constraintTop_toTopOf="@id/image_profile_received"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="상대방 닉네임" />

    <!-- (옵션) 받은 이미지 -->
    <ImageView
        android:id="@+id/image_attachment_received"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="100dp"
        android:minHeight="100dp"
        android:maxWidth="280dp"
        android:maxHeight="280dp"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"
        android:background="@drawable/rounded_image_background"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:visibility="gone"
        app:layout_constraintStart_toEndOf="@id/image_profile_received"
        app:layout_constraintTop_toBottomOf="@id/text_nickname_received"
        tools:src="@drawable/ic_camera"
        tools:visibility="visible"
        android:contentDescription="받은 이미지" />

    <!-- 받은 텍스트 메시지 -->
    <TextView
        android:id="@+id/text_message_received"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:background="@drawable/chat_bubble_received"
        android:padding="12dp"
        android:textColor="@android:color/black"
        android:maxWidth="280dp"
        app:layout_constraintStart_toEndOf="@id/image_profile_received"
        app:layout_constraintTop_toBottomOf="@id/image_attachment_received"
        tools:text="안녕하세요. 물건 아직 있나요? (받은 메시지)" />

    <!-- 받은 메시지 시간 -->
    <TextView
        android:id="@+id/text_timestamp_received"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="10sp"
        android:layout_marginStart="4dp"
        app:layout_constraintBottom_toBottomOf="@id/text_message_received"
        app:layout_constraintStart_toEndOf="@id/text_message_received"
        tools:text="오후 5:37" />

</androidx.constraintlayout.widget.ConstraintLayout>
